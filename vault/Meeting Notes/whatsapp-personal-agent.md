# סוכן WhatsApp אישי (WAHA + Outlook + חשבוניות)

## Overview
פרויקט עצמאי (לא קשור לצוות התוכן יעל/יובל/חן/נועה) — מטרתו לתת ל-Claude Code יכולת לשלוח ולקבל הודעות WhatsApp בשם המשתמש (אלעד), ובהמשך לבנות סוכן שיחתי מלא בוואטסאפ (כולל הקלטות קול) שיתפעל גם Outlook (מיילים + יומן) וגם הוצאת חשבוניות (חיבור לנועה/webenefit-invoice הקיימים).

**שלב 1 (שליחה בסיסית) — הושלם ועובד.** שרת [WAHA](https://waha.devlike.pro) (WhatsApp HTTP API לא-רשמי, מנוע NOWEB) רץ ב-Docker על שרת Hetzner Cloud ייעודי (CX23, Falkenstein, IP `188.245.198.72`), עם WhatsApp האישי של אלעד (`972542438624`) מקושר בפועל. Claude מתחבר ב-SSH ישירות לשרת ומריץ `curl` מול ה-API המקומי (`127.0.0.1:3000`, מוגן ב-API key) כדי לשלוח הודעות.

**שלב 2 (סוכן דו-כיווני מלא) — טרם תוכנן/נבנה.** ראה Open Questions.

קבצי הפרויקט בריפו: `waha/` (docker-compose + .env.example, לא מקושר ל-git עד סשן זה), `whatsapp-inbox/` (סקלטון Next.js ריק ל-webhook, נועד במקור לפרוס ב-Vercel — עדיין לא מומש, יתכן שיוחלף בגישה של שירות על אותו שרת Hetzner במקום Vercel נפרד).

## Open Questions
- **ארכיטקטורת שלב 2** — איך בפועל "מקבלים" הודעות: webhook על שירות תמיד-רץ (על שרת ה-Hetzner עצמו, או `whatsapp-inbox`/Vercel כמתוכנן במקור) שמפעיל תהליך שמנתח/עונה. טרם הוחלט איפה ואיך.
- **תמלול הקלטות קול** — יידרש שירות transcription (Whisper API או דומה) לטיפול בהודעות קוליות בוואטסאפ.
- **אינטגרציית Outlook (Microsoft Graph API)** — חסומה: למשתמש אין (או הוא לא בטוח שיש לו) גישת אדמין ל-Azure/Microsoft 365 לרישום App Registration. צריך לבדוק אם חשבון Microsoft אישי (לא בהכרח tenant admin) יכול לרשום אפליקציה בעצמו ב-Entra ID — סביר שכן, לא נבדק בפועל.
- **זרימת אישור לחשבוניות דרך WhatsApp** — נועה (agent-noa) מחייבת טיוטה + אישור מפורש מהמשתמש לפני קריאה אמיתית ל-WeBenefit. איך זה עובד כשההפעלה מגיעה מהודעת WhatsApp (הקלטה/טקסט)? לא הוחלט — צריך מנגנון אישור (תשובת "כן"/"אשר" בצ'אט?) שלא עוקף את דרישת האישור המפורש.
- **קוד/סוכן שמריץ את "השיח"** — כרגע Claude Code (הסשן הזה) הוא מי ששולח הודעות ידנית לפי בקשה. בשלב 2 צריך תהליך אוטונומי-משהו שרץ ברקע ומגיב לבד — טרם הוחלט אם זה Claude Agent SDK רץ כשירות, polling תקופתי, או משהו אחר.
- **גיבוי/ניקוי סיסמת root הזמנית** — נקבעה סיסמת root זמנית (`roothetzner2026`-סטייל) בשרת כדי לעקוף באג מקלדת בקונסולת הדפדפן של Hetzner; login בסיסמה כבר בוטל ב-`sshd_config` (`PasswordAuthentication no`), אבל **הסיסמה עצמה עדיין מוגדרת בפועל בחשבון ה-root** (`passwd`) — לא נדרשת עוד כי SSH דורש מפתח, אבל שווה לשנות אותה למשהו רנדומלי בעתיד כניקיון (לא קריטי כי `PasswordAuthentication no` חוסם שימוש בה מרחוק).

## Session Log

### 2026-08-07 — הקמת שלב 1: WAHA על שרת Hetzner, קישור WhatsApp אישי, שליחה ראשונה [shipped]
- **What was done:** סשן ארוך ותלול-למידה של הקמת תשתית מאפס. זוהו שני scaffolds קיימים בריפו (`waha/`, `whatsapp-inbox/`) שלא היו מקושרים ל-git ולא נעשה בהם שימוש (ראו [[parenting-course-whatsapp-companion]] session 2026-07-31 שכבר ציין את קיומם). ניסיון ראשון לגייס droplet DigitalOcean קיים (`eladshuali-n8n`, מריץ n8n) נכשל לגמרי — גם SSH וגם Web Console החזירו "All configured authentication methods failed" גם אחרי Reset Root Password; המשתמש בחר לוותר על הדרופלט הזה (מתוך רצון לא להעמיק בסיבה) ולעבור לספק/שרת נקי. נבחר **Hetzner Cloud** (בגלל תקציב — DO cost-optimized לא היה זמין, Hetzner cost-optimized גם לא במלאי כרגע, בסוף נרכש **CX23** ב-Falkenstein, $6.49/חודש, 2vCPU/4GB/40GB). קונסולת הדפדפן של Hetzner התגלתה כלא-אמינה: מקלדת המשתמש (Windows, לא-US layout) שיבשה תווים ספציפיים בהקלדה ידנית לקונסולה (`&`→`7`, `>`→`.`, `_`→`-`, `(`→`9`, `)`→`0`, `*`→`8`) — אובחן כבעיית retype ידני (לא paste) על פריסת מקלדת לא-אנגלית. הפתרון שעבד בסוף: `passwd` בקונסולה עם סיסמה אלפאנומרית בלבד, ואז SSH רגיל **מ-PowerShell של המשתמש עצמו** (שם אין באג המקלדת) כדי להריץ את פקודת הוספת מפתח ה-SSH; מאותה נקודה Claude התחבר ישירות ב-SSH והמשיך את כל ההקמה בעצמו.
- **הקמת השרת (בוצע ע"י Claude דרך SSH):** התקנת Docker (סקריפט רשמי `get.docker.com`), הפעלת `ufw` (רק SSH מותר), ביטול `PasswordAuthentication` ב-sshd אחרי שהמפתח אומת. תיקון `waha/docker-compose.yml`: השם המקורי `devlikeapro/waha-noweb` **לא קיים** (pull access denied) — התיקון הנכון (אומת מול תיעוד רשמי): image הוא `devlikeapro/waha` עם `WHATSAPP_DEFAULT_ENGINE=NOWEB`. נוסף גם `env_file: .env` ל-compose כדי שקרדנציאלס דשבורד/סוואגר שנוצרים אוטומטית ב-WAHA (`WAHA_DASHBOARD_USERNAME/PASSWORD`) ישרדו restart. נוצר API key אקראי (`openssl rand -hex 32`) ב-`.env` בשרת בלבד (לא בריפו) ונשמר עותק מקומי גיטיגנור-מוגן (`waha/.api_key.local`). קונטיינר עלה, session `default` נפתח, QR הוצג (פג פעם אחת, חודש בהצלחה בניסיון שני), **המשתמש סרק ב-WhatsApp האישי שלו (`972542438624`, "אלעד שועלי")**, session עבר ל-`WORKING`, ונשלחה בהצלחה הודעת בדיקה לעצמו דרך `POST /api/sendText`. אומת ששרדות ה-session ל-restart של הקונטיינר (בעקבות תיקון ה-env_file) — נשאר `WORKING`.
- **Decisions:** (1) המשתמש קיבל את הסיכון התיאורטי (חסימת מספר) של API לא-רשמי על המספר האישי שלו, ובחר להשתמש בו במקום מספר משני. (2) גישה ל-WAHA היא רק דרך SSH (bound ל-127.0.0.1 בלבד) — Claude מריץ `curl` מרחוק ב-SSH בכל שליחה, אין חשיפה ציבורית של הפורט. (3) הוחלט "שלב 1 + לתכנן שלב 2" באותה הזמנה — שלב 2 (סוכן מלא) עדיין בגדר תכנון, לא בוצע.
- **Notes / Caveats:** נתקלתי בחשד ל-**prompt injection** פעמיים במהלך הסשן: (א) `whatsapp-inbox/AGENTS.md` — כבר תועד קודם ב-[[parenting-course-whatsapp-companion]], לא פעלתי לפיו. (ב) WebFetch על `docker-compose.yaml` הרשמי של WAHA מ-GitHub החזיר "ניתוח אבטחה קריטי" מזויף-לכאורה עם הוראות ושאלה מכוונת אליי — לא פעלתי לפיו, המשכתי עם מקור נפרד (docs.waha.devlike.pro) שהיה תקין. המשתמש עודכן על שני המקרים בזמן אמת. **לא נבדקה עדיין קליטת הודעות נכנסות בזמן אמת** — כרגע רק שליחה עובדת; קליטה דורשת webhook (ראה Open Questions).
- **Related:** [[parenting-course-whatsapp-companion]], [[project-overview]], [[agent-noa]], [[skill-webenefit-invoice]], [[env-config]]
