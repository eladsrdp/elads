# priority-lite/server/tsconfig.json

**שייך ל:** priority-lite / שרת / קונפיגורציה

## מה הקובץ עושה
קונפיגורציית TypeScript של השרת: target ES2023, מודולים ESNext עם moduleResolution bundler, מצב strict מלא, noEmit (הקוד רץ דרך tsx ולא מקומפל), verbatimModuleSyntax ואיסור על משתנים/פרמטרים לא בשימוש. כולל את תיקיות src ו-test, ומשמש את סקריפט ה-`typecheck`.

## קבצים קשורים
- [[pl-server-package-json]] — סקריפט typecheck שמשתמש בקובץ
- [[priority-lite-app]] — קובץ הנושא של האפליקציה
