# Claude Code local settings (machine-specific, personal)
.claude/settings.local.json

# Environment secrets — never commit real keys (.env.example IS tracked)
.env
hargal-ws/.env

# Yael's output — generated content, not source-controlled
Output/
